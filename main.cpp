#include <iostream>
#include "Interface.hpp"

int main()
{
    /*
    std::cout << "\n\nLina -- (Linear Algebra Calculator)\n\n";

    Interface interface;
    bool running = true;

    do
    {
        running = interface.run();

    } while (running);
    */

    return 0;
}
